<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wayne.exam.dao.RoleDao">
	<select id="queryRole" resultType="com.wayne.exam.entity.Role">
		SELECT id,authority,name,code
		FROM es_role
	</select>
</mapper>